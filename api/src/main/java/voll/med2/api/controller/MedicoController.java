package voll.med2.api.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController // Indica que essa classe vai responder requisições HTTP e retornar dados (normalmente em JSON)
@RequestMapping("medicos") // Define o caminho base para os endpoints dessa classe
public class MedicoController {


}
